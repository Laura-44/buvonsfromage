<h1>Nos propositions de categories :</h1>
<% @categories.each do |category| %>
      <ul>
        <li><%= @categories = Category.where(name: ["beer", "wine", "cheese"]) %></li>
        <%# <li><%= category.food.name</li> %>
        <%# <li><%= category.drink.name</li> %>
      </ul>
    <% end %>


 <%# avec index on itère sur toutes les categories dans la seed et ensuite on recharge la page et on change d'url %>
 <%# .where pour récpérer la clé qui reprend category %>
 <%# séparer  %>
